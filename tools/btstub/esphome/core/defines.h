#pragma once
/* Stand-in for ESPHome's generated defines.h.
 *
 * On a real build this file is written by codegen and carries USE_SPEAKER
 * only when a speaker platform is in the YAML. Here it is empty on purpose:
 * tools/checkbt.py passes -DUSE_SPEAKER for the configurations that want it,
 * so the guard is exercised in both directions rather than being pinned on. */
